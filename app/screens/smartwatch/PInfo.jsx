import { StyleSheet, View } from "react-native";
import Screen from "../Screen";

const PInfo = ({ navigation }) => {
  return (
    <Screen header={{ title: "Personal Information", navigation }}></Screen>
  );
};
export default PInfo;
const styles = StyleSheet.create({});
