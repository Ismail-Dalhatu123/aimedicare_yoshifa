// import { View } from "react-native";

// const MapList = ({ Render, items = [], ...props }) => {
//   return (
//     <View {...props}>
//       {items.map((i, idx) => (
//         <Render item={i} key={idx} />
//       ))}
//     </View>
//   );
// };
// export default MapList;
